

let str = "bbbaacccc"

func charLetterCount(from str: String) -> String {
    var finalString = ""
    var dict = [String: Int]()
    var strArray = Array(str)
    for i in 0..<strArray.count {
        if strArray[i]
    }
    return finalString
}
